import {ImageView} from 'react-native-admob-native-ads';

export const ImageView = ({}) => {
  return (
    <ImageView
      style={{
        width: '100%',
        height: 250,
      }}
    />
  );
};
